# 问题备忘录（y.problems.md）

> 文件职责：问题与远期改进的备忘录，**只增不删、编号递增**（解决后不删条目，在任务清单里闭环）。
> 条目格式：

```markdown
{N}. **{标题}**（{YYYY-MM-DD} {状态}）：

当前问题：{现状描述、为什么是问题}

未来方案：

- {方案要点}（可含设计草稿/代码块/工作量估计）
```

- **状态取值**：记录 / 讨论确认 / 定案 / 待实现
- 条目被任务清单引用（`[problems#N]`），任务完成时回改状态
- 适合记录：暂不做但要留案的设计讨论、远期方向、"遇到特定场景才能推进"的验证项

---

## 一、问题记录（自有条目，2026-09-17 起）

1. **macOS/Linux 平台适配延后**（2026-09-17 定案，待触发；沿系列基线）：

   当前问题：玻璃效果以 Windows 实机为唯一开发/验证平台（沿 CapsulePulse 焦点联动材质配方），当前开发实机只有 Windows；macOS vibrancy / Linux blur / 三端打包在 Windows 版成熟前无法实测，提前做只会积累未经真机验证的代码与虚假的跨平台结论。

   未来方案：

   - Windows 版功能全量优先；代码中平台分支按 `#[cfg(target_os)]` 预留，但只在 Windows 实测
   - 触发条件：Windows 版功能闭环 + 打包分发跑通后立项跨平台适配，逐端实测
   - 玻璃相关改动一律注明"仅 Windows 实机验证"（AGENTS.md 素材与环境陷阱同款约束）

2. **未打包 exe 的系统通知署名为 PowerShell（若引入系统通知）**（2026-09-17 记录，沿系列已知项，随打包自然解决）：

   当前问题：系列实测（CapsulePulse，2026-09-09）：tauri-winrt-notification 经 `CreateToastNotifierWithId(AUMID)` 发送，未安装的裸 exe 无 AUMID 注册时按 crate 官方设计回退 `POWERSHELL_APP_ID`，toast 来源显示为"Windows PowerShell"而非应用名——Tauri 未打包应用的已知限制，非项目 bug。本项目一期不涉及系统通知，此条为引入系统通知或打包分发时的预留案。

   未来方案：

   - 正解 = 打包安装器（NSIS/MSI）：bundler 自动注册快捷方式 + AUMID + 应用图标，toast 即署名 CapsuleTODO 并带正式图标
   - 不提前 hack：手工注册属机器级一次性配置不入库，打包后仍要安装器重新注册（系列拍板接受 dev 期偏差）

3. **paths 落址的地标校验与进程内缓存（借鉴评估，暂缓）**（2026-09-17 记录，沿系列评估结论，待触发）：

   当前问题：系列评估结论（CapsulePulse，2026-09-10）：运行时数据双落址（dev=项目根、release=exe 同级）下，"锚点推导 + 地标校验 + 进程内缓存"三件套中后两件均为负资产——dev 锚点 = CARGO_MANIFEST_DIR 编译期保证，无脚本语言 `__file__` 式的层级偏移失败面；release 挪 exe 与首启空目录皆属合法态，项目根式地标会误伤首启；runtime_root 全进程仅启动期调用，OnceLock 缓存收益为零（KISS）。本项目沿双落址基线，结论同源适用。

   未来方案：

   - 触发条件 ①（高频调用）：路径解析进入热路径（如多命令高频取址）→ runtime_root() 以 `std::sync::OnceLock<PathBuf>` 进程内缓存，一次解析终身复用
   - 触发条件 ②（数据目录防呆）：出现"exe 与数据目录分离/误删"的防护需求（如多实例、误操作恢复提示）→ 以 data/ 目录标记文件校验取代项目根式地标，缺失时给出可读指引而非静默新建

4. **开机自启（远期，一期不做）**（2026-09-17 记录，待触发）：

   当前问题：桌面小板的产品形态天然适合开机自启（随系统进入桌面即常驻）；一期按用户定案不做，先以手动启动使用，观察实际使用频率后再决定是否值得引入。

   未来方案：

   - 触发条件：日常使用中"忘记启动板子"成为真实痛点时立项
   - 方案①（倾向）：tauri-plugin-autostart 插件（官方维护，写系统启动项），是否给设置入口随使用观察定
   - 方案②：系统层手工配置启动项（零代码，一次性机器配置不入库）
