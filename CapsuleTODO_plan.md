# CapsuleTODO — 项目计划书

> 玻璃质感的桌面 Todo 看板：固定在桌面、以玻璃为基底，只呈现 Todo 清单供用户勾选。Tauri 2 + Rust，技术栈沿 CapsulePulse。
> 文档状态：2026-09-17 一期收口回写（§2 定案值、§6 勾结）；二期/三期仍仅记录、不细化。

## 1. 项目概述

CapsuleTODO 是一款固定在桌面上的玻璃 Todo 小程序：

- **核心交互**：玻璃板上的 Todo 清单，随手查看随手勾选（一期全量目标）
- **固定桌面**：常驻桌面一隅，不占注意力、随用随走
- **玻璃基底**：沿系列玻璃材质（平时透明常驻、聚焦真磨砂），深浅色自适应
- **分期演进**：二期临时剪贴板（气泡 + 白板）、三期 AI 规范内容——均仅记录待细化

系列定位：CapsuleRetro（包装游戏）→ CapsulePlan（落定计划）→ CapsulePulse（记录时间）→ **CapsuleTODO（桌面清单）**。

### 项目目标（除产品外）

- **学习 Rust**：全部业务逻辑放 Rust 侧（状态机、持久化、业务规则），前端只做展示——Rust 占比高、可单测
- 用户背景：Python 熟练，Rust 已随 CapsulePulse 入门实战一轮（状态机/SQLite/调度全流程跑通）

## 2. 核心设计（2026-09-17 一期定案）

### 2.1 Todo 数据模型与勾选状态（定案）

- `TodoItem { id, text, done }`：文本非空且 ≤100 字符（越界严格拒绝）；一期操作 = 添加/勾选/删除（**编辑缓到二期**随剪贴板议）
- 勾选后：条目折叠进"已完成"折叠区（默认收起 + "已完成 N" 计数徽章）并呈现**删除线**变淡；排序 = 未完成在前按创建序（id 升序），已完成在后
- 删除不做确认框（一期定案 KISS：条目价值低、误删可承受，使用中觉得误删风险高再翻案）

### 2.2 存储（定案）

- rusqlite（SQLite bundled）落 `data/todo.db`（双落址：dev=项目根、release=exe 同级）；表 `todos(id, text, done)`（不做 created_at——排序按 id 即创建序，KISS）
- **操作即落库，单一事实源 = db**，重启恢复；首启无文件 = 建表正常态，库损坏/SQL 错误严格报错启动失败（不进容错白名单）

### 2.3 窗口与常驻（定案）

- 300×400 **固定尺寸**（resizable false）、无边框透明、**置顶**（alwaysOnTop）
- **全屏让位**：前台应用全屏覆盖板子所在显示器时摘除置顶（被盖住），退出全屏恢复置顶——Rust 侧 1s 轮询判定 + 动态摘挂（extern user32 直连，零新依赖）
- **位置记忆**：窗口销毁/退出时保存 x/y 落 `configs/config.json`，启动恢复；越界兜底回默认位（主屏右下距边 40px）
- 任务栏/Alt+Tab 保持默认显示（暂不隐藏，定案记录）；单实例防双开；开机自启一期不做（y.problems#4）

### 2.4 玻璃效果（沿系列定案）

| 平台    | 实现                                             | 效果                                                                              |
| ------- | ------------------------------------------------ | --------------------------------------------------------------------------------- |
| Windows | DWM 焦点联动材质（extern dwmapi 直连，零新依赖） | 平时 alpha 透明常驻 + 聚焦 Acrylic 真磨砂（✅ CapsulePulse 实测配方，主验证平台） |
| macOS   | vibrancy                                         | 真玻璃（延后，problems#1）                                                        |
| Linux   | blur + 半透明                                    | 依赖 compositor，妥协方案（延后，problems#1）                                     |

- 编译期分支：`#[cfg(target_os = "...")]`，三端互不影响
- 前端玻璃卡片：CSS `backdrop-filter: blur()` 叠加系统级模糊

## 3. 技术栈

| 层   | 选型                           | 理由                                           |
| ---- | ------------------------------ | ---------------------------------------------- |
| 框架 | Tauri 2（Rust + 系统 WebView） | 三端玻璃插件成熟、包体小（~10MB）              |
| 语言 | Rust（后端全部业务）           | 学习目标 + 状态机/SQLite 表达自然              |
| 前端 | Vue 3 + TypeScript + Vite      | 与系列一致，组件化玻璃卡片                     |
| 存储 | rusqlite（SQLite）             | 零配置本地库，`data/todo.db` 双落址            |
| 玻璃 | DWM 焦点联动材质               | 沿 CapsulePulse PL010/PL011 成熟配方，零新依赖 |

## 4. 界面设计（玻璃艺术，定案示意）

```
┌─────────────────────────────────┐
│  CapsuleTODO                    │  ← 无边框透明玻璃小板 300×400，置顶
│                                 │
│  [ 添加一条待办…           + ]  │  ← 添加行（PL002）
│                                 │
│  ☐ 买牛奶                       │  ← 进行中区（未完成在前）
│  ☐ 写周报                       │
│                                 │
│  ▸ 已完成 3                     │  ← 折叠区：默认收起 + 计数徽章
│                                 │  ← 展开后条目删除线变淡，悬停 × 删除
└─────────────────────────────────┘
```

- 无边框全窗拖动；聚焦瞬间 Acrylic 真磨砂、平时 alpha 透明透桌面
- 深色/浅色跟随系统（prefers-color-scheme）
- 布局细节随 PL001/PL002 live 目验微调，定稿线框随收口回写 z.plan 附录

## 5. 工程结构（沿系列目录风格）

```
CapsuleTODO/
├── core/                   # Tauri 2 后端（版本单一来源 Cargo.toml）
│   ├── Cargo.toml
│   ├── tauri.conf.json     # version 省略回落 Cargo.toml
│   ├── capabilities/       # ACL 权限
│   ├── icons/              # 应用图标
│   └── src/
│       ├── lib.rs          # 应用装配：玻璃挂载 + 模块注册
│       ├── main.rs         # 薄入口
│       └── …               # 业务模块与 commands/ 随一期方案落地
├── ui/                     # Vue 前端
│   ├── App.vue
│   ├── types.ts            # IPC DTO 镜像类型（单一来源 = Rust serde）
│   └── components/
├── configs/                # 用户参数 config.json（运行时写入，gitignore）
├── data/                   # 运行时数据（gitignore，运行时自建）
├── assets/                 # 静态资源
└── .agents/skills/         # 项目自建 skill（audit-project/audit-report/progress-task）
```

## 6. 分期规划

### 一期 — 玻璃 Todo 勾选闭环（✅ 2026-09-17 全部收口）

- [x] PL001 玻璃壳与最小清单闭环（工程骨架 / 玻璃焦点联动材质 / 状态机 TDD / 勾选闭环）（2026-09-17 收口）
- [x] PL002 清单持久化与内容管理（SQLite 双落址 / 操作即落库 / 增删 / 折叠区 / 重启恢复）（2026-09-17 收口）
- [x] PL003 桌面固定与一期收口（置顶+全屏让位 / 位置记忆 / 单实例 / 收口）（2026-09-17 收口）
- [x] 首轮全量审计 A001 + FIX001 修复（质量闸门实际前置于收口——用户定案先审计后收口）（2026-09-17 闭环）

（任务条目与做法见 x.progress.md，方案展开见 z.plan.md 附录 PL001–PL003 与 A001；提交 V0.1.0.3 随一期收口统一执行）

（任务条目与做法见 x.progress.md，方案展开见 z.plan.md 附录 PL001–PL003）

### 二期 — 临时剪贴板（仅记录，不细化）

- 临时剪贴板功能：玻璃板上以气泡形式提示用户
- 白板：供用户临时写内容
- 满 5 个气泡提醒清理

### 三期 — AI 规范（仅记录，不细化）

- 接入 AI，规范 Todo 与临时内容

## 7. 时间预估

**全职：约 3–5 天**（PL001 1–2 天 / PL002 0.5–1 天 / PL003 1–2 天，含 live 目验往返）；**兼职（每天 2–3 小时）约 1.5–2.5 周**。工程栈与玻璃配方沿系列成熟定案，无探索期。

## 8. 风险与对策

| 风险                            | 等级   | 对策                                                                                          |
| ------------------------------- | ------ | --------------------------------------------------------------------------------------------- |
| Rust 借用检查学习曲线           | 中     | 状态机/存储先写纯逻辑+单测，小步迭代（系列已实战一轮，经验可复用）                            |
| Linux 玻璃效果依赖 compositor   | 中     | 文档声明局限；半透明+模糊退化为可接受方案                                                     |
| 桌面固定形态与全屏/置顶应用冲突 | 已定案 | 置顶 + 全屏让位（1s 轮询动态摘挂 topmost）；live 实测为准，不足以被盖时降级 hide/show（§2.3） |
| 数据丢失                        | 低     | 操作即持久化；单文件 db 拷贝即备份（双落址沿系列）                                            |

## 9. 与系列项目的关系

| 项目            | 职责           | 技术                      |
| --------------- | -------------- | ------------------------- |
| CapsuleRetro    | DOS 游戏包装   | Tauri + Rust + DOSBox     |
| CapsulePlan     | 计划落定 agent | Pi 扩展 + TypeScript      |
| CapsulePulse    | 工作时间看板   | Tauri + Rust + SQLite     |
| **CapsuleTODO** | 桌面 Todo 看板 | **Tauri + Rust + SQLite** |

CapsuleTODO 沿用 CapsulePulse 的工程骨架、玻璃材质配方与工作流规范（AGENTS/skills 同源），业务域从计时换为清单。
